import styled from 'styled-components';

const FooterStyles = styled.footer`
  clear: both;
  padding-top: 10%;

  .social-icons {
    width: 260px;
    margin: 0 auto;
  }
`;

export default FooterStyles;